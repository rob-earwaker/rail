language: python
python:
  - 2.7
  - 3.6
script:
  - pip install nose coverage coveralls
  - nosetests --with-doctest --with-coverage --cover-package=rail
    --cover-erase --cover-html --doctest-extension=md
    --doctest-options=+ELLIPSIS
after_success:
  - coveralls
